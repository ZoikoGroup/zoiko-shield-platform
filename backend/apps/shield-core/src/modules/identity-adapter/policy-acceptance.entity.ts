import type { PolicyAcceptance as PolicyAcceptanceRow } from '@prisma/client';

/** Row of identity.policy_acceptances, persisted through Prisma. */
export type PolicyAcceptance = PolicyAcceptanceRow;
